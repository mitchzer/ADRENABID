<h1>Auctions#index</h1>
<p>Find me in app/views/auctions/index.html.erb</p>


<ul>
  <% auctions = Auction.all %>
    <% auctions.each do |auction| %>
    <li><%= link_to auction.product.name, auction_path(auction) %></li>
  <% end %>
</ul>
